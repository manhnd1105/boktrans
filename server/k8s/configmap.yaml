apiVersion: v1
kind: ConfigMap
metadata:
  name: boktrans-config
data:
  MODEL_NAME: "kc/openrouter/free"
  MODEL_PROVIDER: "openai"
  MODEL_BASE_URL: "http://localhost:20128/v1"
  JOBS_DIR: "/app/jobs"
